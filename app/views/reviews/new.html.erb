<h2>New Review for <%= @restaurant.name %></h2>

<%= simple_form_for([@restaurant, @review]) do |f| %>
  <%= f.input :content, as: :text %>
  <%= f.input :rating, as: :integer, label: "Rating (0 to 5)" %>
  <%= f.button :submit, "Submit Review", class: "btn btn-primary" %>
<% end %>
